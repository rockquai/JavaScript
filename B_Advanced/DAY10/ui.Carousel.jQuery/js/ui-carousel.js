(function(global, $){
	'use strict';
	// console.log('jquery.fn.carousel', $.fn.carousel);

	// 사용자 옵션 설정
	$('#bs3-headphone').carousel({
		'active_index'    : 0,
		'usign_animation' : true,
		'usign_autoPlay'  : true,
		'rolling_time'    : 1000
	});

})(this, this.jQuery);
